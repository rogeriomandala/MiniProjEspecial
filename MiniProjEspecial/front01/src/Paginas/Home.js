import React from 'react';
import './home.css'; // Assuming the CSS file is in the same directory as this component

const Home = () => {
    return(
        <div>
            <h1>Pagina Home</h1>

        </div>
    )
}

export default Home;